import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { CEFR_LEVELS, getSituationByIndex, SITUATION_COUNT, type CefrLevel } from "@shared/learningWorld";
import { ArrowRight, Compass, Search, Sparkles } from "lucide-react";
import { useMemo, useState } from "react";
import { Link } from "wouter";

const LEVEL_COLORS: Record<CefrLevel, string> = { A1: "bg-emerald-100 text-emerald-800", A2: "bg-teal-100 text-teal-800", B1: "bg-sky-100 text-sky-800", B2: "bg-indigo-100 text-indigo-800", C1: "bg-violet-100 text-violet-800", C2: "bg-rose-100 text-rose-800" };

export default function Situations() {
  const [level, setLevel] = useState<CefrLevel | "all">("all");
  const [search, setSearch] = useState("");
  const [visible, setVisible] = useState(18);
  const matches = useMemo(() => {
    const query = search.trim().toLowerCase();
    return Array.from({ length: SITUATION_COUNT }, (_, index) => getSituationByIndex(index)).filter((item) => item && (level === "all" || item.level === level) && (!query || `${item.title} ${item.goal} ${item.context}`.toLowerCase().includes(query)));
  }, [level, search]);
  const shown = matches.slice(0, visible);

  function chooseSituation(id: string) { sessionStorage.setItem("prize2prideSituationId", id); }

  return <div className="min-h-screen bg-background"><section className="border-b border-border bg-gradient-to-br from-primary via-primary to-accent py-12 text-primary-foreground"><div className="container"><p className="flex items-center gap-2 text-sm font-bold uppercase tracking-[.16em] text-secondary"><Sparkles className="h-4 w-4" /> Prize2Pride practice world</p><h1 className="mt-3 max-w-3xl font-['Playfair_Display'] text-4xl font-bold md:text-5xl">Portuguese for the moments that matter.</h1><p className="mt-4 max-w-2xl text-primary-foreground/80">Explore {SITUATION_COUNT.toLocaleString()} structured practice contexts across A1–C2. Choose one, then continue it with your selected tutor in Arabic, Tunisian Arabic, Portuguese, or English.</p></div></section><main className="container py-8"><div className="rounded-2xl border border-border bg-card p-4 shadow-sm"><div className="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between"><div className="relative w-full lg:max-w-md"><Search className="absolute left-3 top-3 h-4 w-4 text-muted-foreground" /><input value={search} onChange={(event) => { setSearch(event.target.value); setVisible(18); }} className="w-full rounded-xl border border-input bg-background py-2.5 pl-9 pr-3 text-sm outline-none focus:ring-2 focus:ring-primary" placeholder="Search work, housing, health, travel…" /></div><div className="flex flex-wrap gap-2"><button onClick={() => { setLevel("all"); setVisible(18); }} className={`rounded-full px-3 py-1.5 text-xs font-bold ${level === "all" ? "bg-primary text-primary-foreground" : "bg-muted text-muted-foreground"}`}>All</button>{CEFR_LEVELS.map((item) => <button key={item} onClick={() => { setLevel(item); setVisible(18); }} className={`rounded-full px-3 py-1.5 text-xs font-bold ${level === item ? "bg-primary text-primary-foreground" : "bg-muted text-muted-foreground"}`}>{item}</button>)}</div></div><p className="mt-3 text-xs text-muted-foreground">{matches.length.toLocaleString()} situations match your view. Situation cards are structured starting points for practice; tutors will guide the scenario rather than claim it is a certified assessment.</p></div><div className="mt-7 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{shown.map((situation) => situation && <article key={situation.id} className="group rounded-2xl border border-border bg-card p-5 shadow-sm transition hover:-translate-y-0.5 hover:shadow-md"><div className="flex items-start justify-between gap-3"><Badge className={LEVEL_COLORS[situation.level]}>{situation.level}</Badge><span className="grid h-8 w-8 place-items-center rounded-lg bg-primary/10 text-primary"><Compass className="h-4 w-4" /></span></div><h2 className="mt-4 font-['Playfair_Display'] text-xl font-bold text-foreground">{situation.title}</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">{situation.learnerPrompt}</p><p className="mt-4 text-xs font-semibold uppercase tracking-wide text-secondary">{situation.pillar}</p><Link href="/professor"><Button onClick={() => chooseSituation(situation.id)} variant="ghost" className="mt-3 h-auto px-0 text-primary hover:bg-transparent hover:text-primary/75">Practise with a tutor <ArrowRight className="ml-1 h-4 w-4 transition group-hover:translate-x-1" /></Button></Link></article>)}</div>{visible < matches.length && <div className="mt-8 flex justify-center"><Button onClick={() => setVisible((count) => count + 18)} variant="outline">Show 18 more situations</Button></div>}</main></div>;
}
